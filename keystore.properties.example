# Copy this file to keystore.properties (project root) or android/keystore.properties and fill in values.
# Do not commit keystore.properties (it is local-only).

RIDER_RELEASE_STORE_PASSWORD=
RIDER_RELEASE_KEY_ALIAS=rider-release
RIDER_RELEASE_KEY_PASSWORD=
